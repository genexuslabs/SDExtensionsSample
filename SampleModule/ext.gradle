ext {
    ANDROID_COMPILE_SDK_VERSION = 26
    ANDROID_BUILD_TOOLS_VERSION = '26.0.2'
    ANDROID_MIN_SDK_VERSION = 16
    ANDROID_TARGET_SDK_VERSION = 24
    ANDROID_JAVA_VERSION = JavaVersion.VERSION_1_7
    ANDROID_SUPPORT_LIBRARY_VERSION = '24.2.0'
    GOOGLE_PLAY_SERVICES_VERSION = '8.4.0'

    PRE_DEX_LIBS = !project.hasProperty('disablePreDex')
    println('Pre-dex ' + (PRE_DEX_LIBS ? 'enabled' : 'disabled') + '.')
}
